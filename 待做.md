合并两个日期选择器？要么是写两套逻辑，内部判断；要么是写一堆参数，传递适当的参数，有这个必要没有？
electron？
why-did-you-render已经支持react 19，但是其暂时（也可能是永久）不支持react compiler,将持续关注。
移除useMemo，useCallback，让react编译器自行优化。这需要大量的工作。有些use不能删，因为被useeffect使用（还是说这是一个没有必要的优化？）
测试模式，用于在测试后端和生产后端之间切换url。应该简单，只需要一个switch。