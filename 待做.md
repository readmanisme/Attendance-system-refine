合并两个日期选择器？要么是写两套逻辑，内部判断；要么是写一堆参数，传递适当的参数，有这个必要没有？
electron？
why-did-you-render 已经支持 react 19，但是其暂时（也可能是永久）不支持 react compiler,将持续关注。
移除 useMemo，useCallback，让 react 编译器自行优化。这需要大量的工作。有些 use 不能删，因为被 useeffect 使用（还是说这是一个没有必要的优化？）
测试模式，用于在测试后端和生产后端之间切换 url。应该简单，只需要一个 switch。
合并重复代码（存在于几个页面 edit 和 create 界面的错误校验代码）
添加漫游式引导（需要添加很多 ref，而且自动生成的导航栏的 ref 还不好弄)
