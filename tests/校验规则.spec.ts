import { test, expect } from "@playwright/test";
import { Workers_TableName, WorkTypes_TableName } from "./constants";

test.describe("测试工人 校验规则", () => {
  test("创建", async ({ page }) => {});
  test("编辑", async ({ page }) => {});
  test("删除", async ({ page }) => {});
});
test.describe("测试工作 校验规则", () => {
  test("创建", async ({ page }) => {});
  test("编辑", async ({ page }) => {});
  test("删除", async ({ page }) => {});
});
test.describe("测试工时&薪资 校验规则", () => {
  test("显示", async ({ page }) => {});
});
test.describe("测试薪资 校验规则", () => {
  test("创建", async ({ page }) => {});
  test("编辑", async ({ page }) => {});
  test("删除", async ({ page }) => {});
});
test.describe("测试考勤录入 校验规则", () => {
  test("创建", async ({ page }) => {});
  test("编辑", async ({ page }) => {});
  test("删除", async ({ page }) => {});
});
test.describe("测试考勤记录 校验规则", () => {
  test("编辑", async ({ page }) => {});
  test("删除", async ({ page }) => {});
});
